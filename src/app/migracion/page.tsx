import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Proxy Migración Interna | MacroLibre',
  description: 'Modelo VAR + Gravity',
};

export default function MigracionPage() {
  return <main><h1>Migración</h1></main>;
}
